var age = 16;

// age >= 18 ? console.log("Liberado!") : console.log("Bloqueado!");
// var ageChecker = age >= 18 ? true : false;

var ageChecker = age >= 18 ? "Liberado!" : "Bloqueado!";
console.log(ageChecker);